module.exports = function() {
	return {
		union : function(modArray1, modArray2) {
			console.log(modArray1 + modArray2);
			// return p1.concat(p2);
			// all = function(){
			// 	var modfns = [];
			// 	for (var i=0; i < mware.length; i++) {
			// 		modfns.push(mware[i]); 
			// 	}
			// 	return gware.concat(mdfns);
			// };
			// console.log(prod);
			// console.log(all);
			// console.log(all.length);
		}	
	}
}